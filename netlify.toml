[build]
  command = "pnpm run build"
  publish = "build"
  functions = "netlify/functions"


[functions]
  included_files = ["!static/**"]

# Redirects live in _redirects at the repo root — adapter-netlify rejects
# [[redirects]] in this file.

